Run the FoxESS custom component unit tests.

```bash
cd ~/github/home-assistant/core/config/custom_components/foxess-ha && .venv/bin/python -m pytest tests/ -v
```

Report the number of tests passed/failed and any failure details.
