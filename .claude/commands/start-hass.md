Start Home Assistant by running `hass -c config` from the project directory.

Run the following command and stream the output:

```bash
cd ~/github/home-assistant/core && hass -c config
```

Report any startup errors. If Home Assistant starts successfully, confirm it's running and note the URL it's listening on (typically http://localhost:8123).
